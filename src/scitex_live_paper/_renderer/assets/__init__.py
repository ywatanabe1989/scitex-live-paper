"""Vendored static assets for the M1 renderer (PDF.js + CSS + JS overlay).

Files here are committed verbatim from upstream releases. They are *not*
distributed via CDN; the CLI (see issue #7) will copy these into the
output site directory.

Sources
-------
- ``pdfjs/`` — `pdfjs-dist` v4.7.76 (Apache-2.0).
  Origin: https://github.com/mozilla/pdf.js
- ``viewer.css`` / ``viewer.js`` — first-party, this repo's licence.
"""
